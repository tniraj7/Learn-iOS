import SwiftUI

struct Settings: View {
    var body: some View {
        Text("Hello, World!")
    }
}

struct Settings_Previews: PreviewProvider {
    static var previews: some View {
        Settings()
    }
}
